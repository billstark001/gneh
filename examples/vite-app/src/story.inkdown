---
imports:
  ./Badge.mjs:
    default: Badge
    badgeDescription: =
  ./Panel.mjs:
    default: Panel
    description: =
---
:: Start [start]

# Markdown is one source representation

@Badge({ label: "Native view", detail: badgeDescription })

The Panel below is a handwritten `.mjs` passage and is used as a navigation target.

[[Open the native JavaScript Fragment -> Panel({label: "As a page"})]]

[[Open the second local passage -> About]]

:: About
# About

The source module's default `PassageSet` exposes this passage as `passages.About`.

[[Return -> Start]]
