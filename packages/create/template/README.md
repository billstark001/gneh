# gneh story

```sh
npm install
npm run dev
```

Story sources live in `src/story/`. `src/ui.ts` and `src/style.css` are editable application code; replace them when integrating another framework or presentation.

Use `npm run build` for a production Vite build and `npm run preview` to inspect it.
