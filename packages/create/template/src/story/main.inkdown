:: Start [start]
# A new story

This interface is ordinary application code. Change it, replace it with React or Vue,
or mount gneh beside an existing application.

@action visit { @do $visits += 1; }

@view VisitCount(label = "Visits") {
  **{{ label }}:** $visits
  @children
}

[[Visit again => visit]]

@VisitCount("Visits") {
  *The count is stored in story state.*
}

[[Continue -> End]]

:: End
# The next passage

Your story starts here.

[[Return -> Start]]
