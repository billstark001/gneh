# @gneh/renderer-xml

Deterministic host-neutral XML and plain-text projections for tests, tooling, and dialect conformance harnesses.

`XMLRenderer` retains stable activation/change handles for the latest rendered tree. `renderXML` and `renderText` are stateless convenience functions.
